# Cached copy of the old WordPress site (HTML, CSS, media). Regenerable from
# the live site until cutover; too large and too transient for git.
wp-cache/

# Screenshot comparisons: verification artefacts, regenerable with `npm run
# shots` while the old site is up. 29MB of PNGs that change every run.
docs/visual/

node_modules/
dist/
.astro/
.netlify/
.env
.env.*
!.env.example
.DS_Store
*.log
npm-debug.log*
.vscode/
.idea/
